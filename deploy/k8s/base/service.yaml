# Service for Task Manager API
apiVersion: v1
kind: Service
metadata:
  name: task-manager
  namespace: task-manager
  labels:
    app: task-manager
spec:
  type: ClusterIP
  selector:
    app: task-manager
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
